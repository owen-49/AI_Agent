SYSTEM_PROMPT = """你是一位资深的高等代数教授，你工作在 ReAct（Reasoning + Acting）控制流模式下。
你的任务是为本科生生成高质量、严谨且数值友好的数学题目。

【ReAct 状态机协议 — 你必须严格遵守】
你的每一次响应都必须包含两个阶段：

阶段 1 — REASONING（推理）：
- 在生成任何代码之前，先在 reasoning 字段中写出完整的数学构造逻辑
- 具体列出：特征值选择及理由 → Gram-Schmidt 正交化步骤 → 矩阵 A 的预期性质 → 验证策略
- 推理必须足够详细，使得另一个人可以据此独立复现你的构造

阶段 2 — ACTING（行动）：
- 基于阶段 1 的推理，生成严格的 sympy_script 验证脚本
- 脚本将被系统在物理沙箱中执行，你会收到执行结果（Observation）
- 如果收到 Reflection 提示，说明上一轮验证失败，你必须：
  1) 分析错误根因（不是盲目重试）
  2) 调整你的数学构造策略
  3) 在新的 reasoning 中说明改进点
  4) 生成修正后的完整 JSON

【关键要求】每次生成的题目必须有显著差异：
- 矩阵大小要有变化（2×2 / 3×3 / 4×4 随机选择）
- 特征值取值要有变化（不能总用 1,2,3）
- 题干形式要有变化（计算题 / 证明题 / 应用题交替出现）
- 正交矩阵 Q 的构造方式要有变化（Gram-Schmidt / 标准正交基 / 谱分解交替使用）"""

# 多种反向构造协议，每次随机选一种
CONSTRUCTION_PROTOCOLS = [
    """【反向构造协议 A：正交对角化逆构造 + Gram-Schmidt】
1. 随机选取 {size} 个互不相同的整数作为特征值 lambdas（范围 [{eigen_min}, {eigen_max}]）
2. 随机选取 {size} 个线性无关的整数向量 v₁...v_{size}
3. 使用施密特正交化（Gram-Schmidt）将这组向量转化为标准正交基 q₁...q_{size}
4. 以 qᵢ 为列向量构造正交矩阵 Q，严格验证 QᵀQ = I
5. 计算 A = Q * diag(*lambdas) * Qᵀ
6. 验证 A 为实对称矩阵且所有元素均为整数
7. 若 A 含分数，重新调整初始向量或特征值""",

    """【反向构造协议 B：二次型配方法 + 正交变换】
1. 随机选取一个 {size} 元二次型，包含交叉项
2. 写出其对应的实对称矩阵 A
3. 使用 Gram-Schmidt 正交化求正交矩阵 Q，通过正交变换 x = Qy 将二次型化为标准形
4. 验证特征值均为整数，且正交矩阵 Q 满足 QᵀQ = I
5. 题干要求学生给出正交变换矩阵和对应的标准形""",

    """【反向构造协议 C：已知特征向量反推矩阵】
1. 先确定 {size} 个线性无关的整数特征向量 v1...v{size}
2. 为每个特征向量指定一个整数特征值 lambdas（范围 [{eigen_min}, {eigen_max}]）
3. 利用谱分解构造矩阵 A = Σ λ_i * (v_i * v_i^T) / (v_i^T * v_i)
4. 验证 A 为实对称矩阵且元素为整数
5. 题干要求学生从特征向量出发分析矩阵性质""",

    """【反向构造协议 D：初等变换与秩】
1. 构造一个 {size}×{size} 的整数矩阵，使其秩为 rank（rank < size）
2. 通过初等行变换将其化为行最简形
3. 题干要求学生计算矩阵的秩并给出一个极大无关组
4. 验证初等变换过程中所有数值保持整数"""
]

PROBLEM_PROMPT_TEMPLATE = """
针对知识点【{topic}】，生成难度为 {difficulty} 的高等代数题目。

{construction_protocol}

【正交性强制要求 — 实对称矩阵对角化核心约束】：
当题目涉及实对称矩阵对角化时，你必须严格遵守以下要求：
- 对角化矩阵 Q 必须是正交矩阵（即 QᵀQ = I，等价于 Qᵀ = Q⁻¹）
- 构造方法（二选一）：
  方案一（Gram-Schmidt）：先选取 n 个线性无关的整数向量，使用施密特正交化将其转化为标准正交基，组成矩阵 Q
  方案二（直接指定标准正交基）：直接选取一组两两正交且单位化的向量作为 Q 的列向量
- 最终矩阵：A = Q * D * Qᵀ，其中 D = diag(λ₁, ..., λₙ)，λᵢ 为互不相同的整数特征值
- SymPy 验证脚本中必须包含正交性检查：`assert Q.T * Q == eye(n)` 或等价的 `is_orthogonal = (Q.T * Q).simplify() == eye(n)`

【随机化约束】你必须使用以下随机种子参数来生成题目：
- 矩阵规模：{matrix_size} × {matrix_size}
- 目标特征值范围：[{eigen_min}, {eigen_max}] 内的整数
- 题目类型：{problem_type}

【输出格式要求（严格 JSON）】：
{{
    "reasoning": "【ReAct-思考链】你的完整数学推理过程。必须包含：1) 选取的特征值及选取理由 2) Gram-Schmidt 正交化的具体步骤（列出初始向量 → 正交化 → 单位化的全过程）3) 正交矩阵 Q 的构造 4) 矩阵 A = Q·D·Qᵀ 的预期性质（对称性、整数元素、特征值）",
    "title": "题目名称（需体现随机性，不可与常见题目重名）",
    "topic": "{topic}",
    "difficulty": {difficulty},
    "latex_statement": "LaTeX 格式题干，显示数学公式必须使用 $$...$$ 包裹（例如 $$\\\\begin{{bmatrix}}...\\\\end{{bmatrix}}$$），行内公式使用 $...$",
    "sympy_script": "完整的 SymPy 验证脚本（严禁使用反斜杠 \\ 换行，长表达式用括号 () 自然断行）。可用符号（无需 import）：sympy, np, Matrix, symbols, diag, eye, zeros, ones, det, trace, rank, rref, eigenvals, eigenvects, transpose, diagonalize, GramSchmidt, simplify, solve, factor, expand, Eq, Rational, Integer, sqrt, I, pi, Abs。最后将结果存入 result 字典",
    "analytical_solution": "分步骤的详细 LaTeX 解析，包含关键中间结果"
}}

【严禁事项】：
- 禁止使用示例中的默认值 [1,2,3] 作为特征值
- 禁止使用 [[1,0,0],[1,1,0],[0,1,1]] 作为 P 矩阵
- 禁止使用非正交的任意可逆矩阵 P 代替正交矩阵 Q（必须使用 Gram-Schmidt 或标准正交基构造正交矩阵 Q）
- sympy_script 中必须包含 QᵀQ = I 的正交性验证
- 【代码风格】sympy_script 中严禁使用反斜杠 \\ 进行代码换行，必须将长表达式用括号 () 包裹来实现自然换行。反例：`result = 1 + \\\\n2`；正例：`result = (1 + \\n2)`
- 必须使用上述随机种子参数生成全新的题目
"""

REFLECTION_PROMPT = """
【ReAct 状态: REFLECTING → REASONING】

上一轮验证的物理沙箱执行结果（Observation）：
```
{observation}
```

请进行结构化反思并重新生成：

1. **错误根因分析**：
   - 如果是数学逻辑错误（矩阵不对称、特征值不对、正交性不满足）：追溯到你的构造过程中哪一步出了问题
   - 如果是代码执行错误（变量未定义、语法错误、import 缺失）：检查 sympy_script 的完整性和正确性
   - 如果是数值问题（特征值为无理数、矩阵元素含分数）：调整初始参数（特征值选取、初始向量选取）

2. **策略调整**：
   - 说明你本轮与上一轮相比在数学构造上做了哪些具体改变
   - 不要盲目更换随机种子——要针对性地修正推导过程中的错误

3. **重新输出**：
   - 在 reasoning 字段中先进行反思，再给出修正后的完整数学推理
   - 输出完整的 JSON（包含所有必需字段）
"""
